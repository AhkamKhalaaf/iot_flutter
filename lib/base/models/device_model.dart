class DeviceModel
{
  final String name;
  final String id;
  final String description;
  final String imagePath ;
  final bool status;

  DeviceModel(this.name, this.id, this.description, this.imagePath, this.status);
}